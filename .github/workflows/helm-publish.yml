name: Helm Publish

on:
  pull_request:
    branches:
      - main
    paths-ignore:
      - '.git*'
      - 'README.md'
  push:
    branches:
      - 'main'
    paths-ignore:
      - '.git*'
      - 'README.md'
  workflow_dispatch: {}

env:
  CHARTS_PATH: "charts/"
  REGISTRY: "ghcr.io/${{ github.repository }}"

jobs:
  helm:
    permissions:
      contents: write
      packages: write
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v5
      - name: Install Helm
        uses: azure/setup-helm@v4
        with:
          version: v3.18.6
      - name: Setup yq
        uses: dcarbone/install-yq-action@v1
        with:
          version: 'v4.44.3'
      - name: Install chart-releaser
        run: |
          VERSION="1.7.0"
          curl -sSLo cr.tar.gz "https://github.com/helm/chart-releaser/releases/download/v${VERSION}/chart-releaser_${VERSION}_linux_amd64.tar.gz"
          tar -xzf cr.tar.gz
          sudo mv cr /usr/local/bin/cr
          rm -f cr.tar.gz
          cr version
      - name: Configure Git
        run: |
          git config user.name "$GITHUB_ACTOR"
          git config user.email "$GITHUB_ACTOR@users.noreply.github.com"
      - name: Publish
        shell: bash
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
          CR_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: |
          echo "${{ secrets.GITHUB_TOKEN }}" | helm registry login ${REGISTRY} --username ${GITHUB_ACTOR} --password-stdin
          for d in ${CHARTS_PATH}*/ ; do
            if [[ ! -f "${d}Chart.yaml" ]]; then
                echo "${d}Chart.yaml not found. Skipping."
                continue
            fi
            rcVersion="false"
            chartName=$(yq '.name' "$d/Chart.yaml")
            currentVersion=$(yq '.version' "$d/Chart.yaml")
            chartRepositoryPath="oci://$REGISTRY/$chartName"
            echo "🔄 Processing chart $chartName and version $currentVersion"
            if [[ $currentVersion == *"rc"* ]] && [ "${{ github.event_name }}" == "pull_request" ]; then rcVersion="true"; fi
            if [[ $rcVersion == "true" ]] || [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
              echo "Checking release for chart $chartName and current file version $currentVersion"
              set +e  # Disable exit on error
              helm pull "$chartRepositoryPath" --version "$currentVersion"
              chartReleased=$?
              set -e  # Enable exit on error
              if [ $chartReleased -eq 0 ]; then
                echo "🦘 $chartName version already released [skipping]"
              else
                echo "📦 Packaging chart $chartName and version $currentVersion"
                helm dep update "$d"
                helm package "$d" -u -d "${d}"
                chartVersionName="$chartName-$currentVersion"
                chartPackageName="$chartVersionName.tgz"
                echo "Pushing chart $chartPackageName"
                helm push "${d}$chartPackageName" oci://${REGISTRY,,}
                echo "🎉 Chart $chartPackageName pushed successfully"
                if [[ $rcVersion == "false" ]]; then
                  echo "📄 Preparing release notes"
                  changelogUrl="https://github.com/${{ github.repository }}/blob/main/charts/$chartName/CHANGELOG.md"
                  releaseNotesFile="/tmp/release-notes-$chartVersionName.md"
                  echo "See changelog here: [$chartName CHANGELOG.md]($changelogUrl)" > "$releaseNotesFile"
                  echo "📄 Uploading chart to GitHub Pages"
                  cr upload \
                    --owner "$(echo ${{ github.repository }} | cut -d'/' -f1)" \
                    --git-repo "$(echo ${{ github.repository }} | cut -d'/' -f2)" \
                    --package-path "${d}" \
                    --token "$CR_TOKEN" \
                    --release-name-template "$chartVersionName" \
                    --release-notes-file "$releaseNotesFile" \
                    --skip-existing
                  echo "✅ Chart uploaded to GitHub Pages with release notes"
                  echo "📌 Creating git tag $chartVersionName"
                  git tag -a "$chartVersionName" -m "Release $chartVersionName"
                  git push origin "$chartVersionName"
                  echo "✅ Git tag $chartVersionName created and pushed"
                else
                  echo "⏭️  Skipping git tag creation for RC version"
                fi
              fi
            fi
          done

          # Update index.yaml for GitHub Pages
          if [[ "${{ github.ref }}" == "refs/heads/main" ]]; then
            echo "📚 Updating Helm repository index"
            cr index \
              --owner "$(echo ${{ github.repository }} | cut -d'/' -f1)" \
              --git-repo "$(echo ${{ github.repository }} | cut -d'/' -f2)" \
              --charts-repo "https://$(echo ${{ github.repository }} | cut -d'/' -f1).github.io/$(echo ${{ github.repository }} | cut -d'/' -f2)" \
              --index-path ./index.yaml \
              --package-path "${CHARTS_PATH}" \
              --token "$CR_TOKEN" \
              --push
            echo "✅ Helm repository index updated"
          fi
