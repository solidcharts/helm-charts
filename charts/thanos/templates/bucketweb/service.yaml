{{- if and .Values.bucketweb.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "thanos.bucketweb.fullname" . }}
  namespace: {{ .Release.Namespace }}
  labels:
    {{- include "thanos.bucketweb.labels" . | nindent 4 }}
  {{- with .Values.bucketweb.service.annotations }}
  annotations:
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  type: {{ .Values.bucketweb.service.type }}
{{- if (semverCompare ">= 1.31-0" .Capabilities.KubeVersion.Version) }}
{{- with .Values.bucketweb.service.trafficDistribution }}
  trafficDistribution: {{ . }}
{{- end }}
{{- end }}
  ports:
    - name: http
      protocol: TCP
      port: {{ .Values.bucketweb.service.ports.http }}
      targetPort: http
  selector:
    {{- include "thanos.bucketweb.selectorLabels" . | nindent 4 }}
{{- end }}
